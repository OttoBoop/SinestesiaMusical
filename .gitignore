# Python
__pycache__/
*.py[cod]
.pythonlibs/
.cache/

# Node
node_modules/

# Vendored bgutil PO provider — rebuilt via scripts/setup_pot_provider.sh
vendor/

# Downloaded / uploaded audio (regenerated at runtime)
uploads/*.mp3
uploads/*.mp4
uploads/*.m4a
uploads/*.webm
uploads/*.opus
uploads/*.part
uploads/ytdl_*

# Logs
*.log

# Analysis cache (regenerated at runtime; future: Cloudflare R2)
cache/
# ML model weights (downloaded/added out-of-band; not bundled on the free tier)
models/
*.onnx
testassets/
