- [yt-dlp YouTube downloads](youtube-ytdlp-downloads.md) — download failures from this server's IP aren't a bot/IP block; the real fix is a server-side JS runtime (deno + android_vr). Invidious/Piped/cobalt/cookies/OAuth are all dead ends.
- [YouTube download reliability](youtube-downloads.md) — audio-only yt-dlp + deno + auto PO tokens (bgutil); isolated temp dir avoids rename crash; friendly errors + retry.
- [Deployment target choice](deployment-target-choice.md) — app moved off Replit to **Docker on Render** (`Dockerfile` + `render.yaml`); must run as ONE stateful instance with gunicorn `--workers 1` (in-memory job state + background download threads + companion PO-token service); `scripts/start.sh` runs both processes and binds to `$PORT`.
